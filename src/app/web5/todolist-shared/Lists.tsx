import { useEffect } from "react";

const retrieveLists = () => {
	// fetch all the shared todolists
};

export const Lists = () => {
	// fetch all the shared todolists
	useEffect(() => {}, []);
	return (
		<div>
			<h1>Lists</h1>
		</div>
	);
};
